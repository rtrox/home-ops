---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile
helmDefaults:
  cleanupOnFail: true
  wait: true
  waitForJobs: true

releases:
  - name: cilium
    namespace: kube-system
    chart: oci://ghcr.io/home-operations/charts-mirror/cilium
    version: 1.18.6
    # Cilium's own hubble-relay needs CoreDNS to become healthy, but CoreDNS
    # is the next release in this same helmfile and can't come up until
    # cilium's CNI is in place -- don't block bootstrap progression on
    # cilium's full readiness (hubble-relay self-heals once CoreDNS exists).
    wait: false
    waitForJobs: false
    values:
      - "../../../../cluster-apps/mini/kube-system/cilium/app/values.yaml"
    hooks:
      # Apply cilium network configuration
      - events:
          - postsync
        command: kubectl
        args:
          - apply
          - --namespace=kube-system
          - --server-side
          - --field-manager=kustomize-controller
          - --kustomize
          - ../../../../cluster-apps/mini/kube-system/cilium/config/
        showlogs: true

  - name: coredns
    namespace: kube-system
    chart: oci://ghcr.io/coredns/charts/coredns
    version: 1.47.0
    values:
      - "../../../../cluster-apps/mini/kube-system/coredns/app/values.yaml"
    needs:
      - kube-system/cilium

  - name: cert-manager
    namespace: cert-manager
    chart: oci://quay.io/jetstack/charts/cert-manager
    version: v1.21.1
    values:
      - "../../../../cluster-apps/base/cert-manager/app/values.yaml"
    needs:
      - kube-system/coredns

  - name: flux-operator
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-operator
    version: 0.58.1
    values:
      - "../../../../cluster-apps/mini/flux-system/flux-operator/app/values.yaml"
    needs:
      - cert-manager/cert-manager

  - name: flux-instance
    namespace: flux-system
    chart: oci://ghcr.io/controlplaneio-fluxcd/charts/flux-instance
    version: 0.58.1
    wait: false
    values:
      - "../../../../cluster-apps/mini/flux-system/flux-operator/instance/values.yaml"
    needs:
      - flux-system/flux-operator
