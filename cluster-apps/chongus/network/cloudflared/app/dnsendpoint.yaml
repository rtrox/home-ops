apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared-tunnel
spec:
  endpoints:
    - dnsName: cho-ext.rtrox.io
      recordType: CNAME
      targets:
        - ${TUNNEL_ENDPOINT}
