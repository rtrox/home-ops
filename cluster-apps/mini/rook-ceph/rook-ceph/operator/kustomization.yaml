---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml

configMapGenerator:
  - name: rook-ceph-values
    files:
      - values.yaml=../../../../base/rook-ceph/operator/values.yaml

configurations:
  - ./kustomizeconfig.yaml
