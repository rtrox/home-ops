---
apiVersion: v1
kind: Service
metadata:
  name: &name blink
  labels:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: *name
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5432
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: *name
    app.kubernetes.io/instance: *name
